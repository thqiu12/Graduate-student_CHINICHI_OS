# 生产环境 API 地址。
# ⚠️ 不要在仓库里硬编码后端 URL！
# 在 Vercel 项目设置 → Environment Variables → Production 中配置 VITE_API_BASE_URL。
# 仅当 Vercel 中未配置时，构建才会读取本文件的默认值。
# 默认 fallback 故意留空，使前端在生产构建中显式 404，便于尽早发现配置缺失。
VITE_API_BASE_URL=
